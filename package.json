{
  "name": "ipo-prospectus-risk-decoder",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build && cp -r frontend/dist ./dist"
  }
}
